{"doc": "using-rh-entitlements-pipelines-self-managed-reduced.adoc", "section": "Using Red Hat subscriptions in builds", "job_statement": "When I need to build container images that require RHEL packages, I want to provide build processes with access to Red Hat subscription entitlements, so I can install licensed RHEL content during builds", "job_type": "core", "persona": "cluster administrator", "job_map_stage": "Configure", "granularity": "main_job", "parent_job": null, "prerequisites": ["When I manage an OpenShift cluster, I want to ensure my cluster has valid Red Hat subscriptions, so I can access entitled content"], "related_jobs": ["When I build container images in pipelines, I want to use Red Hat Universal Base Images as base images, so I can create supportable containers", "When I manage secrets in OpenShift, I want to understand how entitlement secrets work, so I can troubleshoot access issues"], "desired_outcomes": ["Minimize build failures due to missing package repositories", "Ensure builds can access all entitled RHEL packages", "Reduce time spent troubleshooting subscription issues in builds"], "evidence": "using-rh-entitlements-pipelines-self-managed-reduced.adoc -> Using Red Hat subscriptions in builds, lines 1-15", "notes": "Core job that enables RHEL package installation during image builds"}
{"doc": "using-rh-entitlements-pipelines-self-managed-reduced.adoc", "section": "Adding the etc-pki-entitlement secret to a pipeline service account", "job_statement": "When I have a pipeline that needs to build images with RHEL packages, I want to configure the pipeline service account with entitlement access, so I can ensure the build tasks can authenticate to Red Hat repositories", "job_type": "core", "persona": "cluster administrator", "job_map_stage": "Configure", "granularity": "user_story", "parent_job": "When I need to build container images that require RHEL packages, I want to provide build processes with access to Red Hat subscription entitlements, so I can install licensed RHEL content during builds", "prerequisites": ["When I need to build container images that require RHEL packages, I want to provide build processes with access to Red Hat subscription entitlements, so I can install licensed RHEL content during builds", "When I manage service accounts in OpenShift, I want to understand how to link secrets to service accounts, so I can grant permissions to automated processes"], "related_jobs": ["When I create pipelines, I want to specify which service account the pipeline runs as, so I can control what resources the pipeline can access", "When I troubleshoot pipeline build failures, I want to verify service account permissions, so I can identify missing credentials"], "desired_outcomes": ["Minimize manual intervention in pipeline builds", "Ensure service accounts have exactly the permissions needed", "Reduce risk of credential leakage by limiting service account scope"], "evidence": "using-rh-entitlements-pipelines-self-managed-reduced.adoc -> Adding the etc-pki-entitlement secret to a pipeline service account, lines 16-35", "notes": "User story implementing the main job by configuring service account access"}
{"doc": "using-rh-entitlements-pipelines-self-managed-reduced.adoc", "section": "Adding the etc-pki-entitlement secret to a pipeline service account", "job_statement": "When I run oc edit or oc patch commands, I want to modify service account configurations declaratively, so I can add secret references without manual console navigation", "job_type": "consumption", "persona": "cluster administrator", "job_map_stage": "Configure", "granularity": "procedure", "parent_job": "When I have a pipeline that needs to build images with RHEL packages, I want to configure the pipeline service account with entitlement access, so I can ensure the build tasks can authenticate to Red Hat repositories", "prerequisites": [], "related_jobs": ["When I manage OpenShift resources, I want to use oc commands for configuration changes, so I can automate and version control my changes", "When I edit Kubernetes resources, I want to understand YAML structure for service accounts, so I can make correct modifications"], "desired_outcomes": ["Minimize time to apply configuration changes", "Ensure changes are repeatable and auditable", "Reduce errors from manual console edits"], "evidence": "using-rh-entitlements-pipelines-self-managed-reduced.adoc -> Adding the etc-pki-entitlement secret to a pipeline service account, lines 25-35", "notes": "Procedural task supporting the user story"}
{"doc": "using-rh-entitlements-pipelines-self-managed-reduced.adoc", "section": "Using Red Hat subscriptions in builds (implicit)", "job_statement": "When I configure build environments, I want to mount entitlement secrets as volumes in build pods, so I can make certificate files available to package managers during builds", "job_type": "core", "persona": "developer", "job_map_stage": "Develop", "granularity": "user_story", "parent_job": "When I need to build container images that require RHEL packages, I want to provide build processes with access to Red Hat subscription entitlements, so I can install licensed RHEL content during builds", "prerequisites": ["When I have a pipeline that needs to build images with RHEL packages, I want to configure the pipeline service account with entitlement access, so I can ensure the build tasks can authenticate to Red Hat repositories"], "related_jobs": ["When I write Dockerfiles for RHEL-based images, I want to configure dnf or yum to use entitlement certificates, so I can install packages from Red Hat repositories", "When I design pipeline tasks, I want to declare volume mounts in task specifications, so I can make files available to build containers"], "desired_outcomes": ["Minimize changes required to existing Dockerfiles", "Ensure entitlement certificates are available at expected paths", "Reduce build complexity by handling credentials at infrastructure level"], "evidence": "using-rh-entitlements-pipelines-self-managed-reduced.adoc -> implicit from service account configuration context, lines 16-35", "notes": "Implicit user story; document describes service account linking but the ultimate job is making certs available in build pods"}
{"doc": "using-rh-entitlements-pipelines-self-managed-reduced.adoc", "section": "Prerequisites (implicit)", "job_statement": "When I manage an OpenShift cluster, I want to ensure my cluster has valid Red Hat subscriptions, so I can access entitled content", "job_type": "core", "persona": "cluster administrator", "job_map_stage": "Configure", "granularity": "main_job", "parent_job": null, "prerequisites": [], "related_jobs": ["When I deploy OpenShift, I want to register the cluster with Red Hat Subscription Management, so I can activate entitlements", "When I monitor subscriptions, I want to verify entitlement status, so I can renew before expiration"], "desired_outcomes": ["Ensure continuous access to Red Hat content repositories", "Minimize service disruptions due to expired entitlements", "Reduce compliance risk by maintaining active subscriptions"], "evidence": "using-rh-entitlements-pipelines-self-managed-reduced.adoc -> implicit prerequisite for all entitlement usage, lines 1-10", "notes": "Foundational prerequisite job for using entitlements in any context"}
{"doc": "using-rh-entitlements-pipelines-self-managed-reduced.adoc", "section": "Using Red Hat subscriptions in builds (implicit)", "job_statement": "When I build RHEL-based container images, I want to verify that entitlements are correctly configured, so I can catch authentication issues early in the development cycle", "job_type": "core", "persona": "developer", "job_map_stage": "Troubleshoot", "granularity": "main_job", "parent_job": null, "prerequisites": ["When I need to build container images that require RHEL packages, I want to provide build processes with access to Red Hat subscription entitlements, so I can install licensed RHEL content during builds"], "related_jobs": ["When builds fail with repository access errors, I want to inspect entitlement certificate availability, so I can determine if the issue is credential-related", "When I test pipeline configurations, I want to run trial builds in isolated environments, so I can validate entitlement setup before production use"], "desired_outcomes": ["Minimize failed builds in production pipelines", "Reduce time to identify root cause of build failures", "Ensure consistent build behavior across environments"], "evidence": "using-rh-entitlements-pipelines-self-managed-reduced.adoc -> implicit from troubleshooting context, lines 1-35", "notes": "Implicit troubleshooting job; document does not explicitly cover verification but this is a natural related job"}
{"doc": "using-rh-entitlements-pipelines-self-managed-reduced.adoc", "section": "Adding the etc-pki-entitlement secret to a pipeline service account", "job_statement": "When I manage multiple pipelines across namespaces, I want to understand the scope of service account configurations, so I can determine whether to configure per-namespace or cluster-wide settings", "job_type": "core", "persona": "cluster administrator", "job_map_stage": "Plan", "granularity": "main_job", "parent_job": null, "prerequisites": ["When I design OpenShift multi-tenancy, I want to understand namespace isolation boundaries, so I can enforce appropriate security policies"], "related_jobs": ["When I plan RBAC for pipelines, I want to define least-privilege service accounts, so I can minimize security risks", "When I standardize pipeline configurations, I want to use templates or operators, so I can ensure consistency across teams"], "desired_outcomes": ["Minimize administrative overhead for entitlement management", "Ensure appropriate security boundaries between teams", "Reduce configuration drift across namespaces"], "evidence": "using-rh-entitlements-pipelines-self-managed-reduced.adoc -> implicit from namespace-scoped service account configuration, lines 16-35", "notes": "Implicit planning job; document shows namespace-scoped configuration but does not discuss multi-namespace strategy"}
{"doc": "using-rh-entitlements-pipelines-self-managed-reduced.adoc", "section": "Using Red Hat subscriptions in builds (variant context)", "job_statement": "When I operate self-managed OpenShift clusters, I want to understand how entitlement management differs from managed services, so I can implement the correct configuration approach", "job_type": "core", "persona": "cluster administrator", "job_map_stage": "Reference", "granularity": "main_job", "parent_job": null, "prerequisites": ["When I choose between managed and self-managed OpenShift,I want to understand operational differences, so I can select the appropriate deployment model"], "related_jobs": ["When I manage OpenShift on-premises, I want to handle certificate and key distribution, so I can provide credentials to build processes", "When I compare self-managed vs managed OpenShift, I want to evaluate administrative responsibilities, so I can understand resource requirements"], "desired_outcomes": ["Minimize misconfiguration due to incorrect deployment assumptions", "Ensure compliance with Red Hat support requirements for self-managed clusters", "Reduce learning curve when transitioning between deployment models"], "evidence": "using-rh-entitlements-pipelines-self-managed-reduced.adoc -> document variant is self-managed-specific, lines 1-35", "notes": "Document is explicitly for self-managed variant; this job represents understanding the context"}
{"doc": "using-rh-entitlements-pipelines-self-managed-reduced.adoc", "section": "Using Red Hat subscriptions in builds", "job_statement": "When I architect CI/CD pipelines for containerized applications, I want to integrate Red Hat entitlements into build processes, so I can leverage RHEL ecosystem packages in my application images", "job_type": "core", "persona": "DevOps engineer", "job_map_stage": "Plan", "granularity": "main_job", "parent_job": null, "prerequisites": ["When I design container image build strategies, I want to choose appropriate base images, so I can balance security, support, and functionality"], "related_jobs": ["When I select base images for applications, I want to evaluate RHEL UBI vs community alternatives, so I can make informed tradeoffs", "When I plan CI/CD tooling, I want to assess OpenShift Pipelines capabilities, so I can determine if it meets build requirements"], "desired_outcomes": ["Minimize vendor lock-in while maintaining support options", "Ensure build processes are repeatable and reliable", "Reduce total cost of ownership for container infrastructure"], "evidence": "using-rh-entitlements-pipelines-self-managed-reduced.adoc -> high-level context from document purpose, lines 1-15", "notes": "Higher-level planning job that precedes the configuration jobs; represents the strategic decision to use RHEL content"}
{"doc": "using-rh-entitlements-pipelines-self-managed-reduced.adoc", "section": "Adding the etc-pki-entitlement secret to a pipeline service account (secret naming)", "job_statement": "When I reference the etc-pki-entitlement secret in configurations, I want to use the correct secret name, so I can avoid errors from typos or naming mismatches", "job_type": "consumption", "persona": "cluster administrator", "job_map_stage": "Configure", "granularity": "procedure", "parent_job": "When I have a pipeline that needs to build images with RHEL packages, I want to configure the pipeline service account with entitlement access, so I can ensure the build tasks can authenticate to Red Hat repositories", "prerequisites": [], "related_jobs": ["When I troubleshoot secret reference errors, I want to verify secret names in the namespace, so I can identify naming issues", "When I document configurations, I want to use exact resource names, so I can create accurate runbooks"], "desired_outcomes": ["Minimize configuration errors from naming mistakes", "Ensure configurations work on first attempt", "Reduce time spent debugging reference errors"], "evidence": "using-rh-entitlements-pipelines-self-managed-reduced.adoc -> secret name 'etc-pki-entitlement' referenced in procedure, lines 25-35", "notes": "Procedural detail; the specific secret name is critical for successful configuration"}
{"doc": "using-rh-entitlements-pipelines-self-managed-reduced.adoc", "section": "Using Red Hat subscriptions in builds (security context)", "job_statement": "When I provide entitlement access to build processes, I want to limit credential exposure to only necessary components, so I can minimize security risk from compromised builds", "job_type": "core", "persona": "security engineer", "job_map_stage": "Secure", "granularity": "main_job", "parent_job": null, "prerequisites": ["When I assess security risks in CI/CD pipelines, I want to identify credential exposure points, so I can apply appropriate controls"], "related_jobs": ["When I implement least-privilege access, I want to scope service account permissions narrowly, so I can contain potential breaches", "When I audit pipeline security, I want to review which service accounts have entitlement access, so I can verify appropriate usage"], "desired_outcomes": ["Minimize blast radius of compromised pipeline credentials", "Ensure entitlements cannot be exfiltrated by unauthorized processes", "Reduce compliance risk by limiting credential access"], "evidence": "using-rh-entitlements-pipelines-self-managed-reduced.adoc -> service account scoping implies security considerations, lines 16-35", "notes": "Security-focused job implicit in service account design; document does not explicitly discuss security but scoping service accounts is a security control"}
{"doc": "using-rh-entitlements-pipelines-self-managed-reduced.adoc", "section": "Adding the etc-pki-entitlement secret to a pipeline service account", "job_statement": "When I modify service account configurations, I want to validate changes before deploying to production, so I can prevent disruptions to active pipelines", "job_type": "consumption", "persona": "cluster administrator", "job_map_stage": "Configure", "granularity": "user_story", "parent_job": "When I have a pipeline that needs to build images with RHEL packages, I want to configure the pipeline service account with entitlement access, so I can ensure the build tasks can authenticate to Red Hat repositories", "prerequisites": [], "related_jobs": ["When I test configuration changes, I want to use development or staging environments, so I can identify issues before production impact", "When I roll out infrastructure changes, I want to implement gradual rollout strategies, so I can minimize impact of errors"], "desired_outcomes": ["Minimize production incidents from configuration errors", "Ensure pipeline availability during configuration changes", "Reduce time to detect and remediate configuration issues"], "evidence": "using-rh-entitlements-pipelines-self-managed-reduced.adoc -> implicit from operational context of configuration changes, lines 16-35", "notes": "Operational best practice job implicit in configuration procedure; document does not discuss testing strategy"}
